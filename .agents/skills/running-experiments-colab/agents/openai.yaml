interface:
  display_name: "Run Experiments on Colab"
  short_description: "Run and monitor GPU experiments on Colab"
  default_prompt: "Use $running-experiments-colab to launch and safely collect this experiment on a Colab GPU."
